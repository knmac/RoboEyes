[project]
name = "roboeyes"
version = "0.1.0"
description = "RoboEyes is a Python library that creates smoothly animated robot eyes for GUI displays."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pygame>=2.6.1"]
